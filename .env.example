# Neon Postgres (provisioned by the Vercel integration in production).
# With no DATABASE_URL the site runs in demo mode with fictional content.
# DATABASE_URL=postgres://...

# Demo-mode gate (only used when DATABASE_URL is absent).
PARTY_PASSWORD=change-me

# Optional password for the human /admin UI (separate from the admin API).
# Leave unset to disable /admin.
# ADMIN_UI_PASSWORD=

# OpenRouter — turns a messy plan dump into a reviewed draft.
# Human landing and agent create share this path. Never commit the key.
# OPENROUTER_API_KEY=
